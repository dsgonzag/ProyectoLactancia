<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.packages.prolactancia">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="Android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/icono_app"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".VideoBeneficios2" />
        <activity android:name=".Maps" />
        <activity android:name=".VideoHigiene" />
        <activity android:name=".VideoBeneficios" />
        <activity android:name=".MitosVerdades" />
        <activity android:name=".Donacion" />
        <activity android:name=".AlmacenamientoTraslado" />
        <activity android:name=".MenuExtraccion" />
        <activity android:name=".EstimulacionExtraccion" />
        <activity android:name=".ProtegeLactancia" />
        <activity android:name=".SoloPecho" />
        <activity android:name=".DuermeSuelta" />
        <activity android:name=".AumentarProduccion" />
        <activity android:name=".FrecuenciaSuccion" />
        <activity android:name=".MenuFrecuenciaLactar" />
        <activity android:name=".SuccionPosicion" />
        <activity android:name=".MadreEnferma" />
        <activity android:name=".Abceso" />
        <activity android:name=".Mastitis" />
        <activity android:name=".DuctosObstruidos" />
        <activity android:name=".DolorGrietas" />
        <activity android:name=".PechosCongestionados" />
        <activity android:name=".Alimentacion" />
        <activity android:name=".MenuProblemasLactancia" />
        <activity android:name=".MenuLactancia" />
        <activity android:name=".Higiene" />
        <activity android:name=".Beneficios" />
        <activity android:name=".MenuPrincipal" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>